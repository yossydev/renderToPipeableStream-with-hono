# renderToPipeableStream-with-hono

A renderToPipeableStream dedicated to the node environment was implemented in combination with hono.

## setup

```bash
$ npm i
$ npm run start
$ open http://localhost:3002
```
